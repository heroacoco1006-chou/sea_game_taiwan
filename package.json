{
  "name": "sea-game-taiwan",
  "version": "0.1.0",
  "private": true,
  "description": "大航海福爾摩沙 — 台灣大航海時代歷史教育遊戲",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 5173 --strictPort",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "phaser": "^3.87.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
